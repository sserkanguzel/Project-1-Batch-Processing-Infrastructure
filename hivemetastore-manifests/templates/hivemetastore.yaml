apiVersion: apps/v1
kind: Deployment
metadata:
  name: hive-metastore
  namespace: hive
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hive-metastore
  template:
    metadata:
      labels:
        app: hive-metastore
    spec:
      volumes:
        - name: hive-extra-libs
          emptyDir: {}
        - name: hive-config
          configMap:
            name: hive-site-config

      initContainers:
        - name: setup-hive
          image: apache/hive:3.1.3
          command:
            - /bin/bash
            - -c
            - |
              echo "Downloading required jars..."
              mkdir -p /opt/hive/lib
              wget -P /opt/hive/lib https://repo1.maven.org/maven2/org/apache/hadoop/hadoop-aws/2.7.4/hadoop-aws-2.7.4.jar
              wget -P /opt/hive/lib https://repo1.maven.org/maven2/com/amazonaws/aws-java-sdk-bundle/1.11.271/aws-java-sdk-bundle-1.11.271.jar
              echo "Initializing Hive schema..."
              schematool -dbType postgres -initSchema --verbose
          env:
            - name: HIVE_CONF_DIR
              value: /opt/hive/conf
          volumeMounts:
            - name: hive-extra-libs
              mountPath: /opt/hive/lib
            - name: hive-config
              mountPath: /opt/hive/conf

      containers:
        - name: hive-metastore
          image: apache/hive:3.1.3  # or your custom image
          ports:
            - containerPort: 9083
          env:
            - name: HIVE_CONF_DIR
              value: /opt/hive/conf
          volumeMounts:
            - name: hive-extra-libs
              mountPath: /opt/hive/lib
            - name: hive-config
              mountPath: /opt/hive/conf
---
apiVersion: v1
kind: Service
metadata:
  name: hive-metastore
  namespace: hive
spec:
  type: ClusterIP  # or NodePort/LoadBalancer based on your use case
  selector:
    app: hive-metastore
  ports:
    - name: thrift
      port: 9083
      targetPort: 9083
