apiVersion: v1
kind: ConfigMap
metadata:
  name: trino-config-worker
  namespace: trino
  labels:
    app: trino
    component: worker
data:
  jvm.config: |
    -server
    -Xmx1G
    -XX:+UseG1GC

  config.properties: |
    coordinator=false
    http-server.http.port=8080
    query.max-memory=512MB
    query.max-memory-per-node=256MB
    discovery.uri=http://trino.trino.svc.cluster.local:8080

  node.properties: |
    node.environment=production
    node.id=trino-worker-{{ randAlphaNum 8 | lower }}
    node.data-dir=/data/trino
